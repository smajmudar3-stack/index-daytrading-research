# Never commit credentials
.env
*.key
*secret*

# Market data lives locally -- 16 GB, see 06_data_guide/DATA.md
data/
*.parquet

venv/
__pycache__/
*.pyc
logs/
.DS_Store
.worktrees/

# editable install metadata
*.egg-info/
.pytest_cache/
.ruff_cache/
local.env

# Per-session routing scratch, mirrored from ~/.local/state/tars/route/. Machine-local by
# construction — the file says so itself — and meaningless in anyone else's checkout.
.tars/route-state/
